[program:crm]
command = /projects/crm/project/crm.bash                    ; Command to start app
user = avrama                                                         ; User to run as
stdout_logfile = /var/log/crm_supervisor.log                          ; Where to write log messages
redirect_stderr = true                                                ; Save stderr in the same log
environment=LANG=en_US.UTF-8,LC_ALL=en_US.UTF-8  
autorestart = true

